# 🎯 Hướng Dẫn Đơn Giản - 3 Bước

## Bước 1: Vào thư mục `web/`
- Click vào thư mục `web/` trong `public_html/`
- Bạn sẽ thấy các files: `index.html`, `_next/`, `admin/`, `agent/`, ...

## Bước 2: Chọn tất cả và Move
- **Chọn tất cả** files và folders trong `web/`
- Click nút **"Move"** (hoặc "Cut", hoặc icon mũi tên →)

## Bước 3: Quay lại và Paste
- **Quay lại** `public_html/` (click vào breadcrumb `public_html` hoặc nút ↑)
- Click nút **"Paste"** (hoặc "Dán")
- **Xóa** thư mục `web/` rỗng

## ✅ Kết quả
Sau khi xong, trong `public_html/` sẽ có:
- `index.html` (file chính)
- `_next/` (thư mục)
- `admin/`, `agent/`, `payments/`, ... (các thư mục)
- **KHÔNG còn** thư mục `web/`

## 🆘 Nếu không tìm thấy nút Move
- Click chuột phải vào files đã chọn → chọn "Move" hoặc "Cut"
- Hoặc tìm icon có hình mũi tên (→)

